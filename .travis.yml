---
language: python
python: ["2.7", "3.6"]
dist: trusty
sudo: false
group: beta
cache:
  - pip
  - directories:
      - $HOME/.pre-commit

install:
  - pip install pre-commit

script:
  - pre-commit run --all-files
  - pip install .

notifications:
  email: false
